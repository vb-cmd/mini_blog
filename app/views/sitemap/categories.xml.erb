<?xml version="1.0" encoding="UTF-8"?>
  <urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    <% @categories.each do |category| %>
      <url>
        <loc><%= category_url(category) %></loc>
        <lastmod><%= category.date_updated %></lastmod>
      </url>
    <% end %>
  </urlset>
